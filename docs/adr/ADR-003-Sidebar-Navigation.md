# 📜 ADR-003: Sidebar / NUM Navigation Model (TV)

**Статус:** 🔒 FROZEN  
**Дата:** 2026-02-01  
**Контекст:** Android TV / PWA  
**Затрагиваемые файлы:** `HomePanel.jsx`, `Sidebar.jsx`, `useSpatialNavigation.js`

---

### ⚠️ Scope
Данное ADR описывает **ТОЛЬКО** client-side TV navigation.  
Backend, player intents и non-TV UX не входят в scope.

---

### 🧩 Контекст

Исторически навигация в клиенте развивалась инкрементально:
- часть логики находилась в `Sidebar`
- часть — в `HomePanel`
- часть — в `useSpatialNavigation`
- фокус управлялся как через SpatialEngine, так и через DOM-события

Это привело к:
- нескольким источникам истины
- нестабильному поведению D-pad
- зависаниям фокуса
- регрессиям при добавлении новых экранов

Для приведения поведения к **NUM / Lampa-подобной модели** принято решение о жёсткой централизации навигации.

---

### 🎯 Цель решения

- Один контроллер навигации
- Предсказуемое поведение D-pad
- Изоляция Sidebar от логики фокуса
- Совместимость с Android TV UX-паттернами
- Основа для будущего transform-based scrolling

---

### 🏛️ Архитектурное решение

#### 1. Sidebar становится *чисто визуальным компонентом*

`Sidebar.jsx`:
- ❌ не содержит навигационной логики
- ❌ не обрабатывает клавиши
- ❌ не использует `useSpatialNavigation`, `useSpatialItem`, `useTVNavigation`
- ❌ не хранит state, влияющий на фокус
- ❌ не вызывает `setActiveZone`, `recoverFocus`, `focus()`

Sidebar принимает **только данные и колбэки**.

##### Контракт Sidebar:

```ts
SidebarProps {
  items: SidebarItem[]
  focusedIndex: number
  isExpanded: boolean
  onSelect: (index: number) => void
}
```

Sidebar:
- не знает, где находится фокус
- не решает, когда открываться
- не инициирует навигационные переходы

Sidebar является "тупым" компонентом намеренно.

#### 2. HomePanel — единственный контроллер навигации

В `HomePanel.jsx` вводится единая модель управления:

```javascript
const [activeArea, setActiveArea] = useState('content') 
// 'sidebar' | 'content' | 'overlay'

const [sidebarIndex, setSidebarIndex] = useState(0)
const [isSidebarExpanded, setSidebarExpanded] = useState(false)
```

❗ Эти состояния:
- существуют только в HomePanel
- не дублируются в дочерних компонентах

#### 3. Обработка клавиш — строго централизована

Все события клавиатуры (D-pad):
- обрабатываются только в HomePanel
- **НЕ** обрабатываются в Sidebar, HomeRow, MovieCard и т.д.

##### Логика обработки (NUM-поведение):

**activeArea === 'sidebar'**
- ArrowUp / ArrowDown → изменение `sidebarIndex`
- ArrowRight → `setActiveArea('content')`
- Back: 
  - если `isExpanded === true` → `setSidebarExpanded(false)`
  - иначе → `setActiveArea('content')`

Контент при этом:
- не реагирует на фокус
- не скроллится
- считается неактивным

**activeArea === 'content'**
- Стрелки → стандартная spatial/grid навигация
- ArrowLeft:
  - если фокус на крайнем левом элементе
  - → `setActiveArea('sidebar')`
  - → `setSidebarExpanded(true)`

**activeArea === 'overlay'**
- HomePanel не обрабатывает ввод
- Модальные окна полностью автономны
- При закрытии overlay восстанавливается предыдущий activeArea

#### 4. Управление Spatial Zones

Переключение зон происходит только на основе `activeArea`:

```javascript
if (activeArea === 'sidebar') setActiveZone('sidebar')
else if (activeArea === 'content') setActiveZone('main')
else if (activeArea === 'overlay') setActiveZone('modal')
```

❌ **Запрещено:**
- менять зоны из Sidebar
- дергать зоны из строк или карточек
- вычислять зону по DOM или эвристикам

---

### 📌 Back Navigation Rule (NUM invariant)

1. **Overlay** → закрывается
2. **Sidebar expanded** → collapse (`isExpanded = false`)
3. **Sidebar collapsed** → content (`activeArea = 'content'`)
4. **Content** → стандартный Back stack (History / Exit)

---

### ⚠️ Типовые ошибки (и запреты)

- **Sidebar знает про фокус** → приводит к race conditions и зависаниям
- **Два источника истины** (Sidebar и HomePanel хранят состояние) → недопустимо
- **Локальная обработка стрелок** в HomeRow / MovieCard → неминуемая регрессия
- **Использование recoverFocus после анимаций** → конфликт React render и SpatialEngine
- **Таймауты “для надёжности”** → маскировка архитектурной ошибки

---

### 🔒 Инварианты (НЕ НАРУШАТЬ)

1. Sidebar никогда не управляет фокусом
2. Один `activeArea` в системе
3. Один обработчик клавиш
4. Overlay всегда изолирован
5. Никаких эвристик
6. Никаких side-effect’ов в UI-компонентах

❌ **Запрещены оптимизации навигации "по ощущениям".**  
Любое улучшение должно быть либо:
- подтверждено измерением
- либо оформлено новым ADR

---

### 🧠 Последствия решения

**Положительные:**
- Предсказуемый D-pad UX
- Устойчивость к новым экранам
- Совместимость с NUM / Lampa паттернами
- Основа для transform-based scrolling
- Снижение регрессий

**Ограничения:**
- Требует дисциплины при разработке
- Любые отклонения должны быть осознанными и документированными

---

### 📌 Статус

После внедрения ADR-003:
- любые изменения навигации требуют отдельного ADR
- Sidebar считается архитектурно завершённым
- дальнейшие улучшения выполняются поверх этой модели
