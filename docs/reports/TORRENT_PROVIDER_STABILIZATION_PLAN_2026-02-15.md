# План реализации: стабилизация торрент-провайдеров и безопасный переход на Torznab

Дата: 15 февраля 2026
Последнее обновление: 16 февраля 2026
Статус: Этапы 0–D реализованы и задеплоены на NAS. Остаётся Этап E (cutover).

## Статус этапов

- Этап 0 (Android test harness): ✅ Выполнено
- Этап A (DoH lookup fix): ✅ Выполнено
- Этап B (TorLook flag + RuTracker config state): ✅ Выполнено — deployed 16.02.2026
- Этап C (диагностические скрипты): ✅ Выполнено — deployed 16.02.2026
- Этап F (preflight "живости" magnet + playable ranking): ✅ Выполнено — deployed 16.02.2026
- Этап D (TorznabProvider): ✅ Выполнено — deployed 16.02.2026
- Этап E (cutover): 📝 Запланировано

## 1. Ситуация сейчас (актуализация на 16 февраля 2026)

- После фикса DoH стабильно работает `jacred`, а `rutor` чаще дает контролируемую выдачу.
- `rutracker` в текущем runtime показывает `No cookies received` (ожидаемо без docker env в локальном окружении теста).
- `torlook` дает `empty` и требует managed disable по флагу.
- Ключевая текущая боль сместилась с поиска на этап старта скачивания/стрима:
  - даже при высоком `seeders` в выдаче некоторые magnet дают `0 peers` в swarm и не стартуют в `torrent-stream`.
- По `RuTracker` пользователь подтвердил, что `RUTRACKER_LOGIN` и `RUTRACKER_PASSWORD` заданы в Docker-настройках. Значит ключевой риск не в отсутствии переменных, а в сетевом/интеграционном пути.
- Историческая DoH-ошибка (`Invalid IP address: undefined`) закрыта и больше не является текущим blocker.

## 1.1 Что было подтверждено до фикса DoH (исторический контекст)

Проверки выполнены на `Television_4K(AVD)` через `adb` и реальный вызов API с эмулятора (`10.0.2.2:3000`):

1. Связность Android runtime -> backend подтверждена (`/api/health` отвечает `200`).
2. Поиск, инициированный с Android runtime:
   - `query=ubuntu`: `jacred=empty`, `rutracker/rutor/torlook=error`.
   - `query=dune`: `jacred=ok (50)`, `rutracker/rutor/torlook=error`.
3. Во всех ошибках проблемных провайдеров одна и та же причина:
   - `Invalid IP address: undefined`.
4. На тот момент корневая проблема была в DoH lookup-слое, а не в авторизации RuTracker.

Дополнительно:

- `:app:connectedDebugAndroidTest` запускается на AVD, но падает на шаблонном тесте `ExampleInstrumentedTest` (ожидает старый package `com.getcapacitor.myapp` вместо `com.torserve.pwa`).

## 1.2 Выполнено после валидации (текущий прогресс)

Сделано:

1. Исправлен `lookup` в `server/utils/doh.js`:
   - добавлена поддержка `opts.all`,
   - callback переведен в async (`process.nextTick`),
   - добавлена валидация IP через `net.isIP`,
   - в ветке `resolvedForDNS` установлен `requestOptions.agent = false`.
2. Исправлен Android instrumented test:
   - `client/android/app/src/androidTest/java/com/getcapacitor/myapp/ExampleInstrumentedTest.java`
   - ожидаемый package изменен на `com.torserve.pwa`.
3. Повторная проверка:
   - `./gradlew :app:connectedDebugAndroidTest` -> **PASS**.
   - Android runtime поиск (`10.0.2.2:3000`) больше не падает с `Invalid IP address: undefined`.

Фактические статусы провайдеров после фикса (`query=dune`, runtime from AVD):

- `jacred`: `ok`, `count=50`
- `rutor`: `ok`, `count=30`
- `torlook`: `empty`
- `rutracker`: `error` -> `RuTracker unavailable: No cookies received`

Вывод:

- Критическая DoH-ошибка закрыта.
- Следующий приоритет — отдельная стабилизация login/session для RuTracker и управляемое отключение TorLook по умолчанию.

## 1.3 Что подтверждено мониторингом 16 февраля 2026 (AVD + backend E2E)

Среда мониторинга:

- Android Studio AVD: `Television_4K(AVD)` (`emulator-5554`)
- Backend: `server/index.js` на `0.0.0.0:3000`
- Runtime endpoint для Android: `10.0.2.2:3000`
- Проверка Android harness: `./gradlew :app:connectedDebugAndroidTest` -> **PASS**

Снимок провайдерной диагностики (`/api/providers/diagnostics`):

- `jacred`: `ok`, `lastCount=50`
- `rutor`: `ok`, `lastCount=30`
- `torlook`: `empty`
- `rutracker`: `error`, `lastError=RuTracker unavailable: No cookies received`, после серии сбоев может открывать circuit (`failures=3`)

Наблюдения по торрентам (ключевое):

1. Тестовый magnet из выдачи `dune` с очень высоким заявленным `seeders` (`599`) не стартовал:
   - `POST /api/add` -> `Torrent timeout: no peers found within 90 seconds`.
2. Второй magnet из той же выдачи (длинный tracker list) также дал тот же timeout `90s`.
3. Для торрента в состоянии `numPeers=0` поток не стартует даже после `boostTorrent`:
   - range-запрос к `/stream/:infoHash/:fileIndex` с `--max-time 20` -> `0 bytes`, `http_code=000`.
   - в логах при этом фиксируется корректный boost `maxConnections: 0 -> 65`, но без появления пиров.
4. Для случая, где пиры уже есть (warm state), stream запускается корректно (`HTTP 206`, быстрый ответ на range).

Вывод мониторинга:

- Проблема "видно сотни сидеров, а не качает" воспроизводится.
- Корневая зона риска сейчас не UI/Android bridge, а "живость" конкретного magnet в реальном swarm в момент запроса.
- Нужен preflight перед показом результата как playable и ранжирование с учетом факта доступности пиров, а не только показателей из индекса.

## 2. Подтвержденная техническая причина падения DoH

В `server/utils/doh.js` кастомный `lookup` не учитывает режим `opts.all === true` у Node.js `https.request`.  
Когда Node ожидает массив адресов, а получает строку, появляется `Invalid IP address: undefined`.

Вывод:

- `process.nextTick()` полезен, но сам по себе проблему не решает.
- Нужно одновременно:
  - поддержать сигнатуру для `opts.all`,
  - вызывать callback асинхронно,
  - валидировать IP перед возвратом.

## 3. Цели и ограничения

Цели:

1. Убрать критические падения в сетевом слое (`doh.js`).
2. Стабилизировать агрегатор без поломки текущего рабочего сценария с `jacred`.
3. Подготовить безопасный переход на Torznab (Jackett/Prowlarr) без изменения API контракта для клиента.
4. Повысить вероятность старта стрима с первого выбранного результата через preflight "живости" magnet.

Ограничения:

- Не ломать `/api/v2/search` и текущий UX добавления торрента.
- Не отключать `jacred` до подтвержденной стабильности альтернатив.
- Изменения внедрять поэтапно с rollback-точками.
- Preflight не должен перегружать CPU/RAM (ограничение по параллелизму, timeout, cache).

## 4. План работ (по этапам)

## Этап 0. Починить Android test harness (быстрый)

Файл: `client/android/app/src/androidTest/.../ExampleInstrumentedTest`

Шаги:

1. Обновить ожидаемый package name на `com.torserve.pwa`.
2. Убедиться, что `:app:connectedDebugAndroidTest` зеленый (без учета провайдерной логики).

Зачем:

- Это убирает ложный "красный" статус Android-проверки и позволяет использовать connected-тесты как регрессионный контур.

Статус:

- Выполнено.

## Этап A. Стабилизация DoH и сетевого запроса (критический)

Файл: `server/utils/doh.js`

1. Исправить `requestOptions.lookup`:
   - если `opts.all === true` -> `cb(null, [{ address: dnsIP, family }])`
   - иначе -> `cb(null, dnsIP, family)`
2. Вызывать callback асинхронно (`process.nextTick`).
3. Добавить валидацию IP (`net.isIP`) перед callback.
4. Оставить `requestOptions.agent = false` в ветке `resolvedForDNS` до завершения стабилизации.

Ожидаемый результат:

- `server/debug_doh.js` больше не дает `ERR_INVALID_IP_ADDRESS`.
- В Android runtime поиски через `/api/v2/search` перестают падать по `Invalid IP address: undefined`.

Статус:

- Выполнено (по runtime-проверке на AVD).

## Этап B. Управляемое отключение проблемных провайдеров по умолчанию

Файлы:

- `server/providers/TorLookProvider.js`
- `server/providers/RuTrackerProvider.js`

Шаги:

1. `TorLook` включать только через env-флаг:
   - `TORLOOK_ENABLED=1` -> включен
   - по умолчанию выключен
2. Для `RuTracker` добавить явный статус конфигурации:
   - если нет валидных кредов -> провайдер пропускается как `not_configured`, без попыток логина
   - расширить диагностику (понятная причина в `providers/diagnostics`)

Ожидаемый результат:

- Нет лишних ошибок/троттлинга из-за заранее неработающих провайдеров.

## Этап C. Диагностика и тестовые скрипты

Файлы:

- `server/debug_doh.js`
- `server/test_providers.js`

Шаги:

1. Обновить `debug_doh.js` под новую сигнатуру `lookup`.
2. В `test_providers.js` выводить причины skip (disabled/not_configured), а не только общий fail.
3. Добавить краткий smoke-чек для каждого провайдера после старта.

Ожидаемый результат:

- Быстрое и однозначное определение причины сбоя на конкретном узле.

## Этап F. Фильтрация/ранжирование по "живости" magnet (preflight playable)

Файлы:

- новый: `server/magnetPreflight.js`
- `server/aggregator.js`
- при необходимости: `server/index.js` (диагностический endpoint)

Шаги:

1. Добавить lightweight preflight для top-N результатов выдачи:
   - проверка валидности magnet/infoHash,
   - короткая peer/metadata-проба (ограниченный timeout),
   - фиксация результата как `playable`, `risky`, `dead`.
2. Добавить к результату поиска поля:
   - `playabilityStatus`,
   - `playabilityScore`,
   - `preflight` (`peers`, `checkedAt`, `durationMs`, `source`).
3. Ввести cache preflight (TTL), чтобы избежать повторных тяжелых проб на одинаковый infoHash.
4. Ограничить стоимость preflight:
   - только для top-N (например 8-12),
   - параллелизм 2-3,
   - короткий timeout (например 8-15s).
5. Обновить сортировку агрегатора:
   - сначала `playable`,
   - затем `risky`,
   - `dead` внизу или скрывать из основного списка (configurable).
6. Добавить feature-flag:
   - `MAGNET_PREFLIGHT_ENABLED=1` (по умолчанию включать после smoke).

Ожидаемый результат:

- В топе выдачи чаще оказываются реально стартующие торренты.
- Снижается число кейсов "seeders есть, stream не стартует".

## Этап D. Безопасное подключение Torznab (Jackett/Prowlarr)

Новый файл:

- `server/providers/TorznabProvider.js`

Изменения:

- `server/providers/index.js`
- при необходимости `server/aggregator.js` (без изменения внешнего контракта)

Шаги:

1. Добавить `TorznabProvider` как новый провайдер, по умолчанию выключенный (`TORZNAB_ENABLED=0`).
2. Работать через indexer-specific endpoints Torznab, а не через `all`.
3. На первом этапе отдавать в UI только результаты с готовым `magnet`, чтобы не ломать `POST /api/add` (текущий контракт ожидает magnet URI).
4. Запустить shadow-режим:
   - старые провайдеры остаются активными
   - Torznab участвует в диагностике и сравнении качества/латентности

Ожидаемый результат:

- Появляется второй стабильный источник без регресса для существующего UX.

## Этап E. Постепенный cutover

1. Сначала production-валидация на малом трафике.
2. Затем постепенное повышение веса Torznab.
3. Только после подтверждения стабильности:
   - отключать прямые HTML-парсеры по одному (сначала `torlook`, затем `rutor`, затем `rutracker-direct` при необходимости).

## 5. План проверки

Локально:

1. `node server/debug_doh.js`
   - Критерий: нет `ERR_INVALID_IP_ADDRESS`.
2. `node server/test_providers.js`
   - `rutracker`: login error или успех, но без падения DNS-слоя
   - `rutor`: результаты или контролируемая ошибка
   - `torlook`: корректный skip при disabled

Runtime:

1. Выполнить поиск через `/api/v2/search?query=...`.
2. Выполнить тот же поиск из Android runtime (эмулятор `adb`, endpoint `10.0.2.2:3000`).
3. Проверить `/api/providers/diagnostics`:
   - статусы осмысленные (`ok`, `empty`, `error`, `not_configured`, `disabled`)
   - нет лавинообразного открытия circuit breaker после первой ошибки.
4. Проверить preflight ранжирование (`/api/v2/search?query=...`):
   - у top результатов присутствуют `playabilityStatus` и `playabilityScore`.
5. Проверить сценарий старта:
   - минимум 1 из top-3 результатов по типовым запросам должен начинать range-stream (`HTTP 206`) за ограниченное время (например до 20 секунд в тестовом стенде).

## 6. Rollback

Если после релиза наблюдается деградация:

1. Отключить `TORZNAB_ENABLED`.
2. Отключить `MAGNET_PREFLIGHT_ENABLED`, если preflight создает лишнюю нагрузку.
3. Вернуть только текущий стабильный путь через `jacred`.
4. Сохранить диагностические логи и повторить тесты этапов A-C.

## 7. Критерии готовности (Definition of Done)

1. Ошибка `Invalid IP address: undefined` не воспроизводится.
2. По крайней мере 2 провайдера дают устойчивую выдачу на типовых запросах.
3. `/api/v2/search` и UI добавления торрента работают без изменений контракта.
4. Диагностика провайдеров показывает причину каждого неуспеха без ручного дебага кода.
5. Для типовых запросов preflight снижает долю "мертвых" magnet в верхней части выдачи.
6. Тест range-stream по top результатам показывает воспроизводимый старт без ручного перебора большого числа ссылок.

## 8. Итоги реализации (16 февраля 2026)

### Этап B — Управляемое отключение провайдеров

Изменённые файлы:
- `server/providers/TorLookProvider.js` — добавлен env-flag `TORLOOK_ENABLED=1`, выключен по умолчанию, `disableReason='disabled'`
- `server/providers/RuTrackerProvider.js` — при отсутствии `RUTRACKER_LOGIN`/`PASSWORD` провайдер ставится в `enabled=false`, `disableReason='not_configured'`, без попыток логина
- `server/aggregator.js` — `disableReason` отображается в `getProvidersStatus()` и `getProvidersDiagnostics()`, disabled-провайдеры видны в stats поиска

### Этап C — Диагностические скрипты

Новые файлы:
- `server/debug_doh.js` — резолвит 7 хостов через DoH (jacred, rutracker mirrors, rutor mirrors, torlook, tmdb), отчёт по провайдерам
- `server/test_providers.js` — smoke-тест всех провайдеров с query (default: "dune"), показывает SKIPPED с причиной для disabled, enable hints

### Этап F — Preflight "живости" magnet

Новые файлы:
- `server/magnetPreflight.js` — lightweight magnet probe через `torrent-stream`:
  - `probeMagnet()`: 10 connections, timeout 8s, peer check через swarm.wires
  - `preflightResults()`: обогащает top-N результатов полями `playabilityStatus`, `playabilityScore`, `preflight`
  - Статусы: `playable` (peers > 0 при metadata), `risky` (metadata без peers), `dead` (timeout без peers), `unchecked`, `unknown`
  - Cache по infoHash (TTL 5 min, max 200 entries, LRU eviction)
  - Concurrency: 3 параллельных probe, top-N: 8

Изменённые файлы:
- `server/aggregator.js` — вызов `preflightResults()` после дедупликации, экспорт `getPreflightStats`
- `server/index.js` — preflight stats в `/api/providers/diagnostics`

Env-переменные:
- `MAGNET_PREFLIGHT_ENABLED` — включён по умолчанию, `=0` для отключения
- `PREFLIGHT_TIMEOUT_MS` — timeout probe (default: 8000)
- `PREFLIGHT_CONCURRENCY` — параллелизм (default: 3)
- `PREFLIGHT_TOP_N` — сколько top результатов проверять (default: 8)

### Этап D — TorznabProvider

Новые файлы:
- `server/providers/TorznabProvider.js` — Torznab-совместимый провайдер (Jackett/Prowlarr):
  - Парсинг XML (RSS 2.0 + `<torznab:attr>`) через regex, без внешних зависимостей
  - Только результаты с magnet (не ломает `POST /api/add`)
  - Native `fetch()` с `AbortController` timeout
  - Извлекает `jackettindexer`/`prowlarrindexer` как имя трекера

Изменённые файлы:
- `server/providers/index.js` — импорт, экспорт и регистрация TorznabProvider
- `server/test_providers.js` — enable hint для torznab

Env-переменные:
- `TORZNAB_ENABLED=1` — включить провайдер (выключен по умолчанию)
- `TORZNAB_URL` — URL Torznab API
- `TORZNAB_API_KEY` — API ключ
- `TORZNAB_TIMEOUT` — timeout запроса (default: 15000)
- `TORZNAB_LIMIT` — max результатов (default: 50)

### Верификация на NAS (192.168.1.70)

Контейнер: `pwa-torserve1` — `healthy`

Провайдеры (по `/api/providers/diagnostics`):
| Provider | Enabled | disableReason |
|----------|---------|---------------|
| jacred | true | — |
| rutracker | true | — (креды заданы в Docker env) |
| rutor | true | — |
| torlook | false | disabled |
| torznab | false | disabled |

Preflight: enabled, timeout=8s, concurrency=3, topN=8

Коммит: `75dc102` (feat: stabilize torrent providers (stages B+C+F+D))
