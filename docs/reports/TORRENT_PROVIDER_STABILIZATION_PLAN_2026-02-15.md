# План реализации: стабилизация торрент-провайдеров и безопасный переход на Torznab

Дата: 15 февраля 2026
Статус: Updated after Android runtime validation (AVD)

## Статус этапов

- Этап 0 (Android test harness): ✅ Выполнено
- Этап A (DoH lookup fix): ✅ Выполнено
- Этап B (TorLook flag + RuTracker config state): ⏳ В работе
- Этап C (диагностические скрипты): ⏳ В работе
- Этап D (TorznabProvider): 📝 Запланировано
- Этап E (cutover): 📝 Запланировано

## 1. Ситуация сейчас

- В прод-использовании стабильно работает `jacred`.
- Остальные провайдеры (`rutracker`, `rutor`, `torlook`) часто дают `error` или `empty`, из-за чего система фактически деградирует до одного источника.
- Воспроизведена ошибка сетевого слоя при тесте `server/debug_doh.js`:
  - `TypeError [ERR_INVALID_IP_ADDRESS]: Invalid IP address: undefined`
- По `RuTracker` пользователь подтвердил, что `RUTRACKER_LOGIN` и `RUTRACKER_PASSWORD` заданы в Docker-настройках. Значит ключевой риск не в отсутствии переменных, а в сетевом/интеграционном пути.
- `TorLook` использует нестабильный/неактуальный домен, поэтому должен быть отключен по умолчанию.

## 1.1 Что подтверждено тестом в Android runtime (эмулятор)

Проверки выполнены на `Television_4K(AVD)` через `adb` и реальный вызов API с эмулятора (`10.0.2.2:3000`):

1. Связность Android runtime -> backend подтверждена (`/api/health` отвечает `200`).
2. Поиск, инициированный с Android runtime:
   - `query=ubuntu`: `jacred=empty`, `rutracker/rutor/torlook=error`.
   - `query=dune`: `jacred=ok (50)`, `rutracker/rutor/torlook=error`.
3. Во всех ошибках проблемных провайдеров одна и та же причина:
   - `Invalid IP address: undefined`.
4. Значит корневая проблема сейчас в DoH lookup-слое, а не в авторизации RuTracker.

Дополнительно:

- `:app:connectedDebugAndroidTest` запускается на AVD, но падает на шаблонном тесте `ExampleInstrumentedTest` (ожидает старый package `com.getcapacitor.myapp` вместо `com.torserve.pwa`).

## 1.2 Выполнено после валидации (текущий прогресс)

Сделано:

1. Исправлен `lookup` в `server/utils/doh.js`:
   - добавлена поддержка `opts.all`,
   - callback переведен в async (`process.nextTick`),
   - добавлена валидация IP через `net.isIP`,
   - в ветке `resolvedForDNS` установлен `requestOptions.agent = false`.
2. Исправлен Android instrumented test:
   - `client/android/app/src/androidTest/java/com/getcapacitor/myapp/ExampleInstrumentedTest.java`
   - ожидаемый package изменен на `com.torserve.pwa`.
3. Повторная проверка:
   - `./gradlew :app:connectedDebugAndroidTest` -> **PASS**.
   - Android runtime поиск (`10.0.2.2:3000`) больше не падает с `Invalid IP address: undefined`.

Фактические статусы провайдеров после фикса (`query=dune`, runtime from AVD):

- `jacred`: `ok`, `count=50`
- `rutor`: `ok`, `count=30`
- `torlook`: `empty`
- `rutracker`: `error` -> `RuTracker unavailable: No cookies received`

Вывод:

- Критическая DoH-ошибка закрыта.
- Следующий приоритет — отдельная стабилизация login/session для RuTracker и управляемое отключение TorLook по умолчанию.

## 2. Подтвержденная техническая причина падения DoH

В `server/utils/doh.js` кастомный `lookup` не учитывает режим `opts.all === true` у Node.js `https.request`.  
Когда Node ожидает массив адресов, а получает строку, появляется `Invalid IP address: undefined`.

Вывод:

- `process.nextTick()` полезен, но сам по себе проблему не решает.
- Нужно одновременно:
  - поддержать сигнатуру для `opts.all`,
  - вызывать callback асинхронно,
  - валидировать IP перед возвратом.

## 3. Цели и ограничения

Цели:

1. Убрать критические падения в сетевом слое (`doh.js`).
2. Стабилизировать агрегатор без поломки текущего рабочего сценария с `jacred`.
3. Подготовить безопасный переход на Torznab (Jackett/Prowlarr) без изменения API контракта для клиента.

Ограничения:

- Не ломать `/api/v2/search` и текущий UX добавления торрента.
- Не отключать `jacred` до подтвержденной стабильности альтернатив.
- Изменения внедрять поэтапно с rollback-точками.

## 4. План работ (по этапам)

## Этап 0. Починить Android test harness (быстрый)

Файл: `client/android/app/src/androidTest/.../ExampleInstrumentedTest`

Шаги:

1. Обновить ожидаемый package name на `com.torserve.pwa`.
2. Убедиться, что `:app:connectedDebugAndroidTest` зеленый (без учета провайдерной логики).

Зачем:

- Это убирает ложный "красный" статус Android-проверки и позволяет использовать connected-тесты как регрессионный контур.

Статус:

- Выполнено.

## Этап A. Стабилизация DoH и сетевого запроса (критический)

Файл: `server/utils/doh.js`

1. Исправить `requestOptions.lookup`:
   - если `opts.all === true` -> `cb(null, [{ address: dnsIP, family }])`
   - иначе -> `cb(null, dnsIP, family)`
2. Вызывать callback асинхронно (`process.nextTick`).
3. Добавить валидацию IP (`net.isIP`) перед callback.
4. Оставить `requestOptions.agent = false` в ветке `resolvedForDNS` до завершения стабилизации.

Ожидаемый результат:

- `server/debug_doh.js` больше не дает `ERR_INVALID_IP_ADDRESS`.
- В Android runtime поиски через `/api/v2/search` перестают падать по `Invalid IP address: undefined`.

Статус:

- Выполнено (по runtime-проверке на AVD).

## Этап B. Управляемое отключение проблемных провайдеров по умолчанию

Файлы:

- `server/providers/TorLookProvider.js`
- `server/providers/RuTrackerProvider.js`

Шаги:

1. `TorLook` включать только через env-флаг:
   - `TORLOOK_ENABLED=1` -> включен
   - по умолчанию выключен
2. Для `RuTracker` добавить явный статус конфигурации:
   - если нет валидных кредов -> провайдер пропускается как `not_configured`, без попыток логина
   - расширить диагностику (понятная причина в `providers/diagnostics`)

Ожидаемый результат:

- Нет лишних ошибок/троттлинга из-за заранее неработающих провайдеров.

## Этап C. Диагностика и тестовые скрипты

Файлы:

- `server/debug_doh.js`
- `server/test_providers.js`

Шаги:

1. Обновить `debug_doh.js` под новую сигнатуру `lookup`.
2. В `test_providers.js` выводить причины skip (disabled/not_configured), а не только общий fail.
3. Добавить краткий smoke-чек для каждого провайдера после старта.

Ожидаемый результат:

- Быстрое и однозначное определение причины сбоя на конкретном узле.

## Этап D. Безопасное подключение Torznab (Jackett/Prowlarr)

Новый файл:

- `server/providers/TorznabProvider.js`

Изменения:

- `server/providers/index.js`
- при необходимости `server/aggregator.js` (без изменения внешнего контракта)

Шаги:

1. Добавить `TorznabProvider` как новый провайдер, по умолчанию выключенный (`TORZNAB_ENABLED=0`).
2. Работать через indexer-specific endpoints Torznab, а не через `all`.
3. На первом этапе отдавать в UI только результаты с готовым `magnet`, чтобы не ломать `POST /api/add` (текущий контракт ожидает magnet URI).
4. Запустить shadow-режим:
   - старые провайдеры остаются активными
   - Torznab участвует в диагностике и сравнении качества/латентности

Ожидаемый результат:

- Появляется второй стабильный источник без регресса для существующего UX.

## Этап E. Постепенный cutover

1. Сначала production-валидация на малом трафике.
2. Затем постепенное повышение веса Torznab.
3. Только после подтверждения стабильности:
   - отключать прямые HTML-парсеры по одному (сначала `torlook`, затем `rutor`, затем `rutracker-direct` при необходимости).

## 5. План проверки

Локально:

1. `node server/debug_doh.js`
   - Критерий: нет `ERR_INVALID_IP_ADDRESS`.
2. `node server/test_providers.js`
   - `rutracker`: login error или успех, но без падения DNS-слоя
   - `rutor`: результаты или контролируемая ошибка
   - `torlook`: корректный skip при disabled

Runtime:

1. Выполнить поиск через `/api/v2/search?query=...`.
2. Выполнить тот же поиск из Android runtime (эмулятор `adb`, endpoint `10.0.2.2:3000`).
3. Проверить `/api/providers/diagnostics`:
   - статусы осмысленные (`ok`, `empty`, `error`, `not_configured`, `disabled`)
   - нет лавинообразного открытия circuit breaker после первой ошибки.

## 6. Rollback

Если после релиза наблюдается деградация:

1. Отключить `TORZNAB_ENABLED`.
2. Вернуть только текущий стабильный путь через `jacred`.
3. Сохранить диагностические логи и повторить тесты этапов A-C.

## 7. Критерии готовности (Definition of Done)

1. Ошибка `Invalid IP address: undefined` не воспроизводится.
2. По крайней мере 2 провайдера дают устойчивую выдачу на типовых запросах.
3. `/api/v2/search` и UI добавления торрента работают без изменений контракта.
4. Диагностика провайдеров показывает причину каждого неуспеха без ручного дебага кода.
