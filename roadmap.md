# 🗺️ PWA-TorServe — ROADMAP

> Этот файл — не просто список задач,  
> а **архитектурная память проекта**.
>
> Он фиксирует:
> - зачем были приняты ключевые решения
> - что уже реализовано и стабильно
> - какие шаги логично делать дальше
>
> Roadmap можно расширять,  
> но изменения должны быть осознанными.

---

## 🎯 North Star (Цель проекта)

**PWA-TorServe** — self-hosted middleware для:
- агрегации торрент-источников
- интеллектуального выбора релизов
- стриминга торрентов в PWA / Android TV клиент

Проект:
- ❌ не хостит контент
- ❌ не заменяет Jackett / Prowlarr
- ✅ ориентирован на устойчивую работу в условиях блокировок (РФ)
- ✅ рассчитан на долгосрочное развитие без архитектурного долга

---

## ✅ ЭТАП 1. Unified Network Layer — РЕАЛИЗОВАНО

**Статус:** ✅ Завершено (MVP)

Реализовано:
- единый сетевой слой для всех провайдеров
- DoH (DNS-over-HTTPS) для обхода DNS-блокировок
- retry / timeout стратегии
- mirror rotation
- graceful degradation

---

## ✅ ЭТАП 2. Aggregator + Unified Search Contract — РЕАЛИЗОВАНО

**Статус:** ✅ Завершено

Реализовано:
- `SearchAggregator` как центр системы
- параллельный опрос провайдеров (`Promise.allSettled`)
- partial success как допустимое состояние
- дедупликация по infohash

---

## ✅ ЭТАП 3. Provider Architecture + Core Providers — РЕАЛИЗОВАНО

**Статус:** ✅ Завершено

Реализовано:
- `BaseProvider` и `ProviderManager`
- Источники: Jacred, RuTracker (auth), Rutor, TorLook

---

## ✅ ЭТАП 4. AutoDownloader v2 — РЕАЛИЗОВАНО

**Статус:** ✅ Завершено

Реализовано:
- отказ от привязки к одному источнику
- использование Aggregator
- дедупликация загрузок и выбор лучшего релиза

---

## ✅ ЭТАП 5. Client / PWA Improvements — РЕАЛИЗОВАНО

**Статус:** ✅ Завершено

Реализовано:
- **Backend API v2:** Envelope pattern, нормализация данных.
- **TV-first UX:** Нативная D-pad навигация, Focus Trap.
- **Smart UI:** Фильтры качества, сортировка.
- **Voice Search:** Android Speech Recognition integration.

---

## ✅ ЭТАП 6. Client Architecture Refinement & UX — РЕАЛИЗОВАНО

**Статус:** ✅ Завершено

**Архитектурное решение (Metadata):** Отказались от создания Backend API (`/api/v2/metadata`) в пользу стратегии **"Zero-Cost Frontend Enrichment"**.
Так как клиент (`Poster.jsx`) уже ходит в TMDB/Кинопоиск через прокси за картинками, он получает полный JSON с описанием. Мы будем сохранять эти данные, не нагружая бэкенд.

### 6.1. Metadata Layer (Frontend Only)
> *Описания фильмов без новых запросов.*

**`Poster.jsx` (Data Ingestion):**
- [ ] Изменить логику кэширования: сохранять в `localStorage` расширенный объект.
  - **Важно:** Добавить `timestamp` для корректной LRU-очистки.
  ```json
  {
    "poster": "https://...",
    "backdrop": "https://...",
    "overview": "Текст описания...",
    "rating": 7.8,
    "year": "2024",
    "title": "Название",
    "timestamp": 1704067200000 
  }
[ ] LRU Eviction: Реализовать защиту от переполнения (лимит ~300 записей). Удалять самые старые по timestamp.

⚠️ Архитектурные ограничения:

localStorage используется как best-effort cache.

Отсутствие метаданных никогда не должно ломать UI (Graceful Degradation).

TorrentModal.jsx (Data Presentation):

[ ] Чтение метаданных из localStorage по ключу.

[ ] UI Update:

Красивый Header с Backdrop. Важно: Если backdrop нет, использовать красивый CSS-градиент (Fallback).

Вывод рейтинга (Badge) с цветовой кодировкой (🟢 >7, 🟡 >5, 🔴 <5).

Блок с описанием (overview) с line-clamp и кнопкой "Еще".

6.2. Refactoring: TV Navigation Hook & Main Grid
КРИТИЧНО: Обеспечить навигацию с пульта во всем приложении.

[ ] useTVNavigation Hook:

Инкапсуляция логики обработки нажатий.

Поддержка Grid Navigation (2D: вверх/вниз + влево/вправо).

Автоопределение количества колонок (Responsive).

[ ] Внедрение (Refactoring):

SearchPanel.jsx (Вертикальный список).

TorrentModal.jsx (Список эпизодов).

App.jsx (Главный экран): ⚠️ Добавить управление фокусом для сетки торрентов (сейчас отсутствует).

6.3. UX: Enhanced Provider Health & Accessibility
Понятная диагностика и доступность.

[ ] Interactive Status Chips:

Кликабельные иконки провайдеров (Tooltip с ошибкой).

Баннер "All Sources Failed", если все провайдеры упали (с кнопкой "Диагностика").

Цветовая кодировка: Error (🔴) vs Timeout (🟡).

[ ] Accessibility:

Добавить aria-label для всех кнопок-иконок (Voice Search, Settings, etc).

### 6.4. TorrentModal TV Optimization
> *Модальное окно не должно выходить за пределы экрана TV.*

**Проблема:** При большом количестве серий (>15) список эпизодов выталкивает header с рейтингом за пределы экрана.

**Планируемые решения:**
- [ ] **Fixed Header:** Закрепить header с backdrop/рейтингом, скролл только для списка эпизодов.
- [ ] **Виртуальный скролл:** Рендерить только видимые эпизоды (~10) для производительности.
- [ ] **Collapse по умолчанию:** Скрывать список эпизодов, показывать только "▼ 25 эпизодов" (разворачивается по клику).
- [ ] **Limit Height:** Ограничить max-height списка эпизодов до `40vh`.

---


Дорожная Карта PWA-TorServe v3.0 — ✅ РЕАЛИЗОВАНО
Цель: Создать Netflix-like главную страницу, используя вашу проверенную 5-уровневую систему обхода блокировок, и устранить последние "узкие места" (DNS Poisoning на клиенте).

🏛️ Фаза 1: Архитектурный Фундамент (Refactoring)
Вы победили блокировку в компоненте Poster.jsx, но теперь эту логику нужно использовать везде (в поиске, в подборках, в деталях фильма). Копировать код нельзя.

✅ Задача 1.1: Создание tmdbClient.js (Unified Fetcher)
Выносим логику из Poster.jsx в универсальную утилиту.

Файл: client/src/utils/tmdbClient.js

Логика:

Принимает endpoint (напр. /trending/all/week).

Прогоняет через каскад (Custom Worker -> Lampa Proxy -> CapacitorHttp -> Corsproxy -> Kinopoisk).

Улучшение: Добавляем Client-side DoH (см. Фазу 3), чтобы починить CapacitorHttp в РФ.

Возвращает нормализованный объект { results: [...], source: 'tmdb' | 'kp' }.

✅ Задача 1.2: Hardcoded Genre Map
TMDB в списках возвращает только ID жанров ([28, 878]), а не названия. Чтобы не делать лишние запросы:

Файл: client/src/utils/genres.js

Действие: Создать статический словарь: { 28: "Боевик", 878: "Фантастика", ... }.

📺 Фаза 2: Главная Страница (Discovery)
Реализация интерфейса "как в Lampa/Netflix" без бэкенда.

✅ Задача 2.1: Клиентское API (discover.js)
Импорт: Использует tmdbClient.js из Фазы 1.

Категории:

🔥 Тренды (/trending/all/week)

🎬 Популярные фильмы (/movie/popular)

📺 Сериалы (/tv/popular)

⭐ Топ рейтинга (/movie/top_rated)

Фильтрация: Удалять людей (media_type: person) и контент без постеров.

✅ Задача 2.2: Компонент HomeRow.jsx (Горизонтальный список)
Виртуализация: Рендерить только видимые + 2 соседних постера (критично для TV с малой памятью).

Навигация:

Использовать useTVNavigation с параметром loop: true.

При фокусе на постере показывать Backdrop на фоне (красивый эффект Lampa).

✅ Задача 2.3: Интеграция с TorServe
Логика клика:

Если фильм: Искать по названию + год.

Если сериал: Искать по названию + S01 (как обсуждалось, чтобы найти пак сезона).

🛡️ Фаза 3: Client-Side DNS-over-HTTPS (Новая фича)
В Акте 11 диагностики вы заметили, что CapacitorHttp падает с ошибкой подключения к 127.0.0.1 из-за DNS Poisoning провайдером. Мы исправили это на сервере (Акт 6), теперь исправим на клиенте.

✅ Задача 3.1: Реализация resolveIP на клиенте
Идея: Перед запросом CapacitorHttp узнать реальный IP через Google/Cloudflare DNS API (они обычно не блокируются).

Код (пример):

JavaScript

async function resolveClientIP(hostname) {
   // Запрос к Google DNS API
   const res = await fetch(`https://dns.google/resolve?name=${hostname}&type=A`);
   const data = await res.json();
   return data.Answer?.[0]?.data; // Реальный IP (напр. 65.8.11.22)
}
✅ Задача 3.2: Внедрение в CapacitorHttp
Логика:

Получаем IP для api.themoviedb.org.

Делаем запрос на https://<IP>/3/....

Передаем заголовок Host: api.themoviedb.org.

Результат: CapacitorHttp начнет работать в РФ без VPN (так как мы обошли отравленный DNS), что даст SSL-шифрование и скорость нативного кода.

⚡ Фаза 4: Кинопоиск как полноценный Fallback (Расширение)
Сейчас Кинопоиск используется только для картинок. Но если TMDB ляжет полностью, главная страница будет пустой.

✅ Задача 4.1: Адаптер Discovery для Кинопоиска
В discover.js: Если все зеркала TMDB недоступны, переключаться на API Кинопоиска.

Эндпоинты KP:

Тренды -> /api/v2.2/films/collections?type=TOP_POPULAR_ALL

Топ -> /api/v2.2/films/collections?type=TOP_250_MOVIES

Маппинг: Преобразовать ответ KP в формат TMDB (id, title, poster_path), чтобы UI не заметил подмены.

🏁 Итоговая Архитектура v3.0
Эта структура гарантирует работу приложения в любых условиях блокировок.

UI Layer: HomePanel -> HomeRow -> Poster

Data Layer: discover.js (Кэширование + Логика категорий)

Network Layer (tmdbClient.js):

🔄 Попытка 1: Custom Worker (Для зарубежья).

🔄 Попытка 2: Lampa Proxy (MVP для РФ).

🔄 Попытка 3: CapacitorHttp + Client DoH (Нативный обход DNS poisoning).

🔄 Попытка 4: Corsproxy.io (Браузерный fallback).

🔄 Попытка 5: Кинопоиск API (Полная замена источника данных).

Fail-safe: Градиенты (Offline mode).

НЮАНС (обязательно учти)

Когда ты делаешь:

https://<IP>/3/...
Host: api.themoviedb.org


⚠️ Возможные проблемы:

TLS SNI

сертификат может не совпасть

✅ Как сделать безопасно

Использовать ТОЛЬКО HTTPS

Проверить, что CapacitorHttp:

не валидирует CN строго
(обычно он доверяет системе)

📌 Правило:

Client DoH — только для CapacitorHttp,
❌ никогда не для browser fetch

Ты это логически уже соблюдаешь — просто зафиксируй.
🗺️ ROADMAP v3.1: Operation "Unstoppable" & TV Resurrection
Version: 3.1
Status: 🚦 READY FOR DEV
Target Platform: Android TV / Smart TV / PWA
Focus: Resilience (Anti-Censorship), Navigation (D-Pad), UX Performance.

🤖 AI AGENT INSTRUCTION: CONTEXT VERIFICATION
STOP! READ THIS BEFORE EXECUTING.
Before implementing any task from this roadmap, you MUST perform a Pre-Work Context Scan:
 * Scan the file structure: Verify if files mentioned (e.g., useTVNavigation.js, server/utils/doh.js) exist or are empty.
 * Check git status or recent commits: Ensure no one else has implemented these features recently.
 * Validate assumptions: If a task says "Code is missing," but you see the code, update the roadmap status and ask the user for a new direction.
 * Propose Improvements: If you see a cleaner architectural solution than described below (e.g., using a newer library or pattern), propose it to the user before writing code.

📅 Execution Batches
Мы выполняем задачи пакетами (Batches) для стабильности сборки.

📦 Batch 1: "Resurrection" (TV Core)
Критический этап. Без него приложение неработоспособно на ТВ.
 * FIX-01a: 1D List Navigation (Базовая механика + CSS Focus state)
 * FIX-01b: 2D Grid Navigation (Для карточек)
 * FIX-01c: Modal Focus Trap (Удержание фокуса)
 * SEC-01: Global Error Boundary

📦 Batch 2: "Unstoppable" (Network Resilience)
Архитектура выживания. Защита от блокировок CDN и DNS.
 * ARC-01: Server-Side DoH Rotation + Circuit Breaker
 * ARC-02: Client Waterfall Fetching (NAS -> Proxy -> Direct -> Gradient UI)

📦 Batch 3: "Polish" (UX & Performance)
Улучшение отзывчивости и информативности.
 * UX-02: Server-Side Badge Parsing (Backend API update)
 * UX-01: SearchPanel Performance (Debounce + Memo)
 * UX-03: Client-Side Sorting & Filtering

📦 Batch 4: "Validation"
 * TEST-01: Real Device Testing

📋 Detailed Task List

| ID | Task Name | Priority | Complexity | Description & Tech Stack |
|---|---|---|---|---|
| FIX-01a | 1D List Navigation | MUST | Mid | Stack: React Hook (useTVNavigation). <br>Desc: Реализовать обработку ArrowUp/ArrowDown. **Важно: Добавить CSS-класс `.focused` для активного элемента.** Поддержка скролла через scrollIntoView. |
| FIX-01b | 2D Grid Navigation | MUST | High | Stack: React Hook. <br>Desc: Добавить логику columns > 1. Обработка ArrowLeft/ArrowRight. Умный переход между строками (сохранение столбца при переходе вниз). |
| FIX-01c | Modal Focus Trap | MUST | Mid | Stack: React (useEffect, DOM). <br>Desc: При открытии модалки (TorrentModal) фокус должен захватываться внутри неё и возвращаться на кнопку открытия при закрытии. |
| SEC-01 | Error Boundary | SHOULD | Low | Stack: React Class Component. <br>Desc: Обернуть приложение, чтобы краш навигации не вешал весь TV-интерфейс (белый экран). |
| ARC-01 | Server DoH Rotation | DONE | Mid | Stack: Node.js (fetch). <br>Desc: В doh.js внедрен массив провайдеров (Google, Quad9, Cloudflare). Логика Race с таймаутом 2сек + Circuit Breaker. |
| ARC-02 | Client Waterfall | DONE | High | Stack: React (AbortSignal). <br>Desc: В tmdbClient.js 5 уровней: Worker -> Lampa Proxy -> CapacitorHttp (DoH) -> Direct -> Gradient. |
| UX-01 | Search Performance | DONE | Low | Stack: React 18 (useDeferredValue) OR use-debounce. <br>Desc: Убрать лаги ввода. Отложенная фильтрация списка результатов. |
| UX-02 | Backend Badges | DONE | Mid | Stack: Node.js (Express). <br>Desc: Перенесен парсинг (4K, HDR, HEVC) на сервер. /api/v2/search возвращает поле badges[] и health. |
| UX-03 | Client Sort/Filter | DONE | Low | Stack: React (useState). <br>Desc: UI для сортировки (Seeds/Size) и фильтрации. Связать с данными из UX-02. |
| TEST-01 | Device Testing | DONE | Low | Stack: Android TV / FireStick. <br>Desc: Тест на реальном железе. Fix: Navbar access, Focus clipping, Scroll jitter, Back button logic. |
| OPT-01 | List Virtualization | WONT | High | Отложено. react-window пока не внедряем, решаем проблемы через UX-01. |

🛠️ Technical Specifications (Reference for AI)

1. Navigation Logic (FIX-01)
Hook должен поддерживать containerRef и itemSelector.
Визуал: Используй CSS transform (scale) или border для фокуса.
Скролл: `el.scrollIntoView({ behavior: 'smooth', block: 'center' })` чтобы избежать скачков.

// Expected Logic Pattern for 2D
const nextIndex = (current, key, cols) => {
   if (key === 'ArrowDown') return current + cols;
   if (key === 'ArrowRight' && (current % cols !== cols - 1)) return current + 1;
   // ... handle boundaries
}

2. Resilience Architecture (ARC-01/02)
Server-Side:
Не использовать хардкод 1.1.1.1. Использовать массив DOH_PROVIDERS.
Client-Side:
// Waterfall Strategy
try {
  return await fetchLocalNAS(query);
} catch {
  console.warn("NAS unreachable, trying Cloudflare Worker...");
  try {
     return await fetchExternalWorker(query);
  } catch {
     return generateGradientPlaceholder(query); // Final Fallback
  }
}

3. Badge Parsing (UX-02 - Server Side)
В файле server/utils/helpers.js (или server/providers/BaseProvider.js) создать:
function extractMetadata(title) {
  return {
    badges: [
      /2160p|4k/i.test(title) && '4K',
      /hevc|x265/i.test(title) && 'HEVC'
    ].filter(Boolean),
    // ...
  }
}

End of Roadmap