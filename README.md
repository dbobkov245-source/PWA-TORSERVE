# PWA-TorServe MVP

Монолитный домашний медиа-сервер для TV-Box (Ugoos).
Стек: Node.js (Express), React (Vite), WebTorrent, LowDB.

## 🚀 Инструкция по запуску (Финал)

Предполагаем, что вы находитесь в корневой папке проекта `pwa-torserve`.

### Шаг 1. Установка зависимостей
Нужно установить пакеты и для сервера, и для клиента.

```bash
# 1. Корневые зависимости (сервер)
npm install

# 2. Клиентские зависимости
npm run client:install
```

### Шаг 2. Сборка Фронтенда
React нужно превратить в HTML/JS файлы, которые сервер сможет отдавать.

```bash
npm run client:build
```
После этого в папке `client/dist` появятся файлы.

### Шаг 3. Запуск Сервера
Теперь запускаем наш "комбайн".

```bash
npm start
```

### Как проверить?
1.  **В браузере (PC/Mobile)**: Открой `http://<IP-UGOOS>:3000`. Ты должен увидеть интерфейс PWA.
2.  **Добавь торрент**: Вставь Magnet-ссылку.
3.  **Дождись метаданных**: Появится список файлов.
4.  **Нажми Play**:
    *   Если ты на ПК, он попробует открыть ссылку, но ничего не произойдет (или браузер предложит открыть приложение).
    *   Если ты на Android (телефоне/TV), система спросит "Чем открыть?" — выбери **Vimu**.

## 📱 PWA и Vercel (Для генерации APK)

Этот проект можно использовать для создания PWA-приложения через [PWABuilder](https://www.pwabuilder.com/).

### Деплой Frontend на Vercel
Так как Vercel не поддерживает длительные процессы (торрент-клиент), мы деплоим туда **только Frontend** для получения HTTPS ссылки, которую примет PWABuilder.

1.  Импортируйте проект в Vercel.
2.  В настройках проекта укажите:
    *   **Framework Preset**: Vite
    *   **Root Directory**: `client` (Нажмите Edit рядом с Root Directory)
3.  Нажмите **Deploy**.

### Создание APK
1.  Возьмите полученный URL (например, `https://pwa-torserve.vercel.app`).
2.  Вставьте его на сайте [PWABuilder](https://www.pwabuilder.com/).
3.  Сгенерируйте и скачайте APK.

### ⚠️ Важное примечание
PWA, запущенное с Vercel (HTTPS), по умолчанию не сможет обращаться к вашему локальному серверу (HTTP) из-за ограничений безопасности браузеров (Mixed Content).
*   **Для тестов**: Используйте браузер на TV-Box и заходите прямо на `http://localhost:3000`.
*   **Для APK**: Установите APK на TV-Box. Если API не работает, убедитесь, что в коде `client/src/App.jsx` прописан правильный IP адрес сервера, или используйте локальный запуск.

## 🛠 API Эндпоинты

*   `GET /api/status` - Статус торрентов (скорость, прогресс).
*   `POST /api/add` - Добавить торрент (body: `{ "magnet": "..." }`).
*   `GET /stream/:infoHash/:fileIndex` - Стриминг файла (поддерживает Range requests).

## 📦 Установка APK на TV-Box

1.  **Перенос файла**:
    *   Скачайте APK файл, созданный в PWABuilder.
    *   Запишите его на USB-флешку или загрузите в облако (Google Drive, Яндекс.Диск), доступное на TV-Box.

2.  **Установка**:
    *   Вставьте флешку в TV-Box (или откройте облако через файловый менеджер).
    *   Найдите файл `.apk` и нажмите на него.
    *   Если появится запрос на разрешение установки из неизвестных источников — **Разрешите** (Настройки -> Безопасность -> Неизвестные источники).
    *   Нажмите **Установить**.

3.  **Первый запуск и Настройка**:
    *   Откройте установленное приложение **TorServe**.
    *   **Важно**: Так как APK загружает интерфейс с Vercel (HTTPS), а ваш сервер работает локально (HTTP), приложение может не увидеть сервер сразу.
    *   **Решение**:
        *   Убедитесь, что сервер запущен на TV-Box (`npm start`).
        *   Если приложение не подключается, самый надежный способ — открыть браузер на TV-Box (Chrome, Firefox) и перейти по адресу `http://localhost:3000`. Браузер предложит "Установить приложение" (Добавить на главный экран), что создаст точно такой же PWA, но работающий напрямую с локальным сервером без проблем с HTTPS/HTTP.
