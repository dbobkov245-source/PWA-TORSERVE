🏴‍☠️ PWA-TorServe: Headless Edition

Статус: Активная разработка (v2.0)
Режим: Vibe Coding (Fast & Efficient)

🎯 Идеология Проекта

Превращение TV-бокса Ugoos AM8 в автономный домашний медиа-сервер ("черный ящик"), управляемый удаленно.

Разгрузка TV: Sony TV используется только как монитор. Вся нагрузка (P2P, хеширование, I/O) лежит на Ugoos.

Выбор: Система интегрируется в Android TV как нативное приложение. При открытии торрента в Lampa/NUM пользователь выбирает: TorrServe Matrix или PWA-TorServe.

Контекст: Мы работаем с существующей кодовой базой (не начинаем с нуля).

🛠️ Архитектура

1. Сервер (The Brain)

Устройство: Ugoos AM8 (Standard, не Pro).

Ресурсы: 4GB RAM (Критично! Экономим память).

Среда: Termux + Node.js (v20+).

Режим: Headless (без монитора, управляется по SSH/HTTP).

Хранилище: Synology NAS, смонтированный через NFS в папку /mnt/media_rw/... (или аналогичную).

Автозапуск: Termux:Boot скрипт, поднимающий сервер при подаче питания.

Функции:

Скачивание торрентов (torrent-stream).

Стриминг по HTTP (Range requests).

Удаление файлов и очистка папок.

API для управления.

2. Клиент (The Face)

Устройство: Sony TV (Android TV) / Смартфоны.

Технология: React + Vite (Frontend) -> Capacitor (Android Container).

Формат: .apk файл (устанавливается на ТВ).

Функции:

Красивый UI каталога и управления.

Intent Filter: Перехват ссылок magnet:? из других приложений.

Отправка задачи на сервер Ugoos.

Запуск нативного плеера (Vimu) через Deep Link.

📅 Roadmap (План действий)

Этап 1: Фундамент (Server Side) ✅

[x] Базовый сервер на Express + torrent-stream.

[x] Монтирование NFS (Synology) на Ugoos.

[x] Реализация Sequential Download (последовательная скачка).

[ ] Настройка Termux:Boot: Автозапуск сервера при ребуте (без участия пользователя).

[ ] Оптимизация под 4GB RAM: Проверка утечек памяти, ограничение пиров.

Этап 2: Трансформация Клиента (APK & Intents) 🚧

[ ] Capacitor Init: Превращение веб-сайта в Android проект.

[ ] Intent Filter: Настройка AndroidManifest.xml для перехвата magnet схем.

[ ] Обработка ссылок: Логика в React (App.jsx), которая ловит входящий URL при запуске приложения.

[ ] Сборка APK: Билд и установка на Sony TV через ADB.

Этап 3: Полировка UX ✨

[ ] Remote Control: Управление воспроизведением с телефона (отправить на ТВ).

[ ] Smart Cleaning: Кнопка "Удалить просмотренное" (физическое удаление с NAS).

[ ] Метаданные: Подтягивание постеров (опционально).

📝 Полезные команды (Cheat Sheet)

SSH Connect: ssh -p 8022 u0_a203@192.168.1.88 (IP может меняться, если не статика)

Запуск Dev сервера (Ugoos): npm start (или node server/index.js)

Сборка React (Client): npm run build

Capacitor Sync: npx cap sync android

Проверка NFS в Termux: df -h